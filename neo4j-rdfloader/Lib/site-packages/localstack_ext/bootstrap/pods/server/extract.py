from typing import Union
import requests
from localstack import config
from requests import Response
def extract_state(content=False):
	B=f"{config.get_edge_url()}/_localstack/pods/state";A=requests.get(B)
	if content:return A.content
	return A